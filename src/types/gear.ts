export type Answers = {
  distance: string;
  terrain: string;
  temp: string;
  night: string;
  experience: string;
  budget: string;
  sweat: string;
  stomach: string;
  feetWidth: string;
  priority: string;
};

export type KitSummary = {
  title: string;
  subtitle: string;
  totalItems: number;
  categories: string[];
  totalCost: number;
};
